{
  "name": "@jolt/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Jolt UI framework-agnostic core: prop schemas + animation primitives (the anti-drift core).",
  "exports": {
    ".": "./src/index.ts",
    "./webgl/particles": "./src/webgl/particles.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@jolt/tokens": "workspace:*",
    "gsap": "^3",
    "three": "^0.185.0",
    "zod": "^3"
  },
  "devDependencies": {
    "@types/three": "^0.185.0"
  }
}

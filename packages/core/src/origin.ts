/**
 * The published origin of the Jolt UI site + jsrepo registry — the single source
 * for both the registry base (`installInfo`) and the site's `site:` URL (used for
 * canonical links, Open Graph, and the sitemap). Provisional Cloudflare Pages
 * default; the Phase 4 deploy swaps this **one** value if the final project name
 * (or a custom domain) differs.
 */
export const JOLT_ORIGIN = 'https://jolt-ui.pages.dev';

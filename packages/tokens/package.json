{
  "name": "@jolt/tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Jolt UI design language: shared CSS-variable tokens + keyframes (the single source of visual identity).",
  "exports": {
    ".": "./src/index.ts",
    "./theme.css": "./src/theme.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}

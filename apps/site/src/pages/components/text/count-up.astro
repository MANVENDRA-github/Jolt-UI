---
import ComponentsLayout from '../../../layouts/ComponentsLayout.astro';
import Breadcrumbs from '../../../components/Breadcrumbs.astro';
import FrameworkSwitcher from '../../../components/FrameworkSwitcher.astro';
import CodeTabs from '../../../components/CodeTabs.astro';
import InstallBlock from '../../../components/InstallBlock.astro';
import PropsTable from '../../../components/PropsTable.astro';
import ComponentPager from '../../../components/ComponentPager.astro';
import { CountUp as ReactCountUp } from '@jolt/react';
import { CountUp as VueCountUp } from '@jolt/vue';
import { CountUp as SvelteCountUp } from '@jolt/svelte';
import { countUpSchema, countUpMeta } from '@jolt/core';
import reactSource from '../../../../../../packages/react/src/components/CountUp/CountUp.tsx?raw';
import vueSource from '../../../../../../packages/vue/src/components/CountUp/CountUp.vue?raw';
import svelteSource from '../../../../../../packages/svelte/src/components/CountUp/CountUp.svelte?raw';

const sourceTabs = [
  { label: 'React', lang: 'tsx', code: reactSource },
  { label: 'Vue', lang: 'vue', code: vueSource },
  { label: 'Svelte', lang: 'svelte', code: svelteSource },
];
const previewText = 'text-4xl font-bold tabular-nums';
---

<ComponentsLayout
  title="CountUp — Jolt UI"
  description="A number that animates to its target with GSAP — for React, Vue, and Svelte."
>
  <Breadcrumbs id="count-up" />
  <h1 class="font-display text-3xl font-semibold tracking-tight">CountUp</h1>
  <p class="mt-2 text-[var(--jolt-text-muted)]">
    A number that animates to its target on scroll — GSAP, three frameworks.
  </p>

  <div class="mt-8">
    <FrameworkSwitcher id="count-up">
      <div class={previewText} slot="react">
        <ReactCountUp to={12500} separator="," client:visible />
      </div>
      <div class={previewText} slot="vue">
        <VueCountUp to={12500} separator="," client:visible />
      </div>
      <div class={previewText} slot="svelte">
        <SvelteCountUp to={12500} separator="," client:visible />
      </div>
    </FrameworkSwitcher>
  </div>

  <InstallBlock meta={countUpMeta} />

  <section class="mt-12">
    <h2 class="font-display text-xl font-semibold">Source</h2>
    <p class="mt-2 text-sm text-[var(--jolt-text-subtle)]">
      Identical behavior, idiomatic per framework.
    </p>
    <div class="mt-3"><CodeTabs tabs={sourceTabs} /></div>
  </section>

  <PropsTable schema={countUpSchema} />
  <ComponentPager id="count-up" />
</ComponentsLayout>

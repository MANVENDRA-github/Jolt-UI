---
import ComponentsLayout from '../../../layouts/ComponentsLayout.astro';
import CodeTabs from '../../../components/CodeTabs.astro';
import InstallBlock from '../../../components/InstallBlock.astro';
import { Tactile as ReactTactile } from '@jolt/react';
import { Tactile as VueTactile } from '@jolt/vue';
import { Tactile as SvelteTactile } from '@jolt/svelte';
import { propsTable, tactileSchema, tactileMeta } from '@jolt/core';
import reactSource from '../../../../../../packages/react/src/components/Tactile/Tactile.tsx?raw';
import vueSource from '../../../../../../packages/vue/src/components/Tactile/Tactile.vue?raw';
import svelteSource from '../../../../../../packages/svelte/src/components/Tactile/Tactile.svelte?raw';

const props = propsTable(tactileSchema);
const sourceTabs = [
  { label: 'React', lang: 'tsx', code: reactSource },
  { label: 'Vue', lang: 'vue', code: vueSource },
  { label: 'Svelte', lang: 'svelte', code: svelteSource },
];
const previewBox =
  'flex h-32 w-full items-center justify-center rounded-lg border border-white/10 bg-black/20';
---

<ComponentsLayout
  title="Tactile Button — Jolt UI"
  description="A raised button that presses down on click — CSS-only, for React, Vue, and Svelte."
>
  <h1 class="text-3xl font-bold">Tactile</h1>
  <p class="mt-2 opacity-80">
    A real <code class="rounded bg-white/10 px-1">&lt;button&gt;</code> — a raised button that presses
    down with a collapsing shadow when you click it — pure CSS, one design language, three frameworks.
    The label is the button's text (children/slot, or the
    <code class="rounded bg-white/10 px-1">label</code> prop); click and
    <code class="rounded bg-white/10 px-1">disabled</code> are forwarded. Under reduced-motion the press
    is instant.
  </p>

  <section class="mt-10 space-y-8">
    <div>
      <h2 class="text-xs uppercase tracking-widest opacity-50">React</h2>
      <div class={previewBox}><ReactTactile label="Get started" /></div>
    </div>
    <div>
      <h2 class="text-xs uppercase tracking-widest opacity-50">Vue</h2>
      <div class={previewBox}><VueTactile label="Get started" /></div>
    </div>
    <div>
      <h2 class="text-xs uppercase tracking-widest opacity-50">Svelte</h2>
      <div class={previewBox}><SvelteTactile label="Get started" /></div>
    </div>
  </section>

  <InstallBlock meta={tactileMeta} />

  <section class="mt-12">
    <h2 class="text-xl font-semibold">Source</h2>
    <p class="mt-2 text-sm opacity-70">
      Pure CSS; renders a native <code class="rounded bg-white/10 px-1">&lt;button&gt;</code> and forwards
      click / disabled / other native attributes.
    </p>
    <div class="mt-3"><CodeTabs tabs={sourceTabs} /></div>
  </section>

  <section class="mt-12">
    <h2 class="text-xl font-semibold">Props</h2>
    <table class="mt-3 w-full border-collapse text-left text-sm">
      <thead>
        <tr class="border-b border-white/15">
          <th class="py-2 pr-4 font-semibold">Prop</th>
          <th class="py-2 pr-4 font-semibold">Type</th>
          <th class="py-2 pr-4 font-semibold">Default</th>
          <th class="py-2 font-semibold">Description</th>
        </tr>
      </thead>
      <tbody>
        {
          props.map((p) => (
            <tr class="border-b border-white/10">
              <td class="py-2 pr-4 font-mono">{p.name}</td>
              <td class="py-2 pr-4 font-mono opacity-80">{p.type}</td>
              <td class="py-2 pr-4 font-mono opacity-80">{p.default ?? 'required'}</td>
              <td class="py-2 opacity-80">{p.description}</td>
            </tr>
          ))
        }
      </tbody>
    </table>
  </section>
</ComponentsLayout>
